/**
 * Utilidad para limpiar datos de onboarding del localStorage
 * Ejecutar una vez para migrar completamente a BD
 */

export function cleanupOnboardingLocalStorage(): void {
  // Sistema de onboarding eliminado — función vacía
}

/**
 * Función para ejecutar automáticamente la limpieza una sola vez
 */
export function runOnboardingMigration(): void {
  // Sistema de onboarding eliminado — no ejecutar nada
}